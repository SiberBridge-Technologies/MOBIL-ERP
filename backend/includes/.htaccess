# Apache 2.4+ (güncel sürüm)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2 ve öncesi (eski sürüm) için yedek
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
